/**
 * Empty module entry. The runtime content of this package is
 * `cordis.patch.yml`, which the profile loader applies as one ordered layer.
 *
 * @module @deepseek-ai/dsh-cluster-bundle
 */

export {}
